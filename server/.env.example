PORT=5000
MONGODB_URI=mongodb://localhost:27017/mentor-tracker
NODE_ENV=development

# Deployed frontend URL(s) allowed to call this API (comma-separated for several).
#   CLIENT_URL=https://your-frontend-domain.com
# When unset, CORS stays open (local development / same-origin production serving).
CLIENT_URL=
